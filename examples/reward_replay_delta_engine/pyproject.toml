[project]
name = "reward-replay-delta-engine"
version = "0.1.0"
description = "CocoIndex example: incrementally replay reward logic over stored RL trajectories."
requires-python = ">=3.11"
dependencies = ["cocoindex>=1.0.7"]

[tool.setuptools]
packages = []
